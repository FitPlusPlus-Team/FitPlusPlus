import React from "react";
import { Link, withRouter } from "react-router-dom";

const Homepage = () => {
    return (
        <Link to="/workout">temp link to workouts</Link>
    )
}

export default Homepage
